interface:
  display_name: "Ask Me"
  short_description: "필요한 결정만 짧게 질문하고 실행 방향을 함께 맞춥니다"
  default_prompt: "Use $ask-me to clarify the few decisions that could materially change this task before proceeding."
policy:
  allow_implicit_invocation: false
